define(function (require) {

    var React = require('react');

    var header = React.createClass({
    	
    	render() {
            return (

                <div id="header">
                    This is the header
                </div>

                
            );
        }
    });

    return header;
});
