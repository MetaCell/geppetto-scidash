import { connect } from 'react-redux';
import Scidash from './Scidash';


const mapStateToProps = state => {
    return {}
}

const mapDispatchToProps = dispatch => {
    return {}
}

const ScidashContainer = connect(
    mapStateToProps,
    mapDispatchToProps
)(Scidash)


export default ScidashContainer;
